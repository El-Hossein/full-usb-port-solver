# full-usb-port-solver

# SETUP:

```git clone https://github.com/El-Hossein/full-usb-port-solver.git
cd full-usb-port-solver
python3.7 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```